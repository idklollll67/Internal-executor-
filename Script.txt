-- BSH Hub (Complete Updated Version with Sleek UI & Ultimate FPS Boost)

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local VirtualUser = game:GetService("VirtualUser")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local ConfigFolderName = "BSHHub_Configs"

pcall(function()
    if not isfolder(ConfigFolderName) then makefolder(ConfigFolderName) end
end)

local ParentGui = (gethui and gethui()) or LocalPlayer:WaitForChild("PlayerGui")

if ParentGui:FindFirstChild("BSHInternalHub") then
    ParentGui.BSHInternalHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BSHInternalHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = ParentGui

-- MODERN NOTIFICATION SYSTEM
local NotifContainer = Instance.new("Frame")
NotifContainer.Size = UDim2.new(0, 240, 0, 250)
NotifContainer.Position = UDim2.new(1, -250, 1, -270)
NotifContainer.BackgroundTransparency = 1
NotifContainer.Parent = ScreenGui

local NotifLayout = Instance.new("UIListLayout")
NotifLayout.SortOrder = Enum.SortOrder.LayoutOrder
NotifLayout.VerticalAlignment = Enum.VerticalAlignment.Bottom
NotifLayout.Padding = UDim.new(0, 8)
NotifLayout.Parent = NotifContainer

local function ShowNotification(message, color)
    task.spawn(function()
        local notif = Instance.new("Frame")
        notif.Size = UDim2.new(1, 0, 0, 36)
        notif.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
        notif.BackgroundTransparency = 1
        notif.Parent = NotifContainer

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 8)
        corner.Parent = notif

        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(45, 45, 55)
        stroke.Transparency = 1
        stroke.Parent = notif

        local indicator = Instance.new("Frame")
        indicator.Size = UDim2.new(0, 3, 1, -12)
        indicator.Position = UDim2.new(0, 6, 0, 6)
        indicator.BackgroundColor3 = color or Color3.fromRGB(120, 100, 255)
        indicator.BorderSizePixel = 0
        indicator.Parent = notif

        local indCorner = Instance.new("UICorner")
        indCorner.CornerRadius = UDim.new(1, 0)
        indCorner.Parent = indicator

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -20, 1, 0)
        label.Position = UDim2.new(0, 16, 0, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.Text = message
        label.TextColor3 = Color3.fromRGB(230, 230, 240)
        label.TextSize = 10
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = notif

        local ts = game:GetService("TweenService")
        local ti = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
        ts:Create(notif, ti, {BackgroundTransparency = 0.05}):Play()
        ts:Create(stroke, ti, {Transparency = 0.3}):Play()

        task.wait(2.5)

        local fadeOut = ts:Create(notif, ti, {BackgroundTransparency = 1})
        ts:Create(stroke, ti, {Transparency = 1}):Play()
        ts:Create(label, ti, {TextTransparency = 1}):Play()
        fadeOut:Play()
        fadeOut.Completed:Connect(function()
            notif:Destroy()
        end)
    end)
end

-- SLEEK FLOATING TOGGLE BUTTON (DRAGGABLE)
local OpenCloseBtn = Instance.new("TextButton")
OpenCloseBtn.Size = UDim2.new(0, 42, 0, 42)
OpenCloseBtn.Position = UDim2.new(0, 15, 0.5, -21)
OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(16, 16, 20)
OpenCloseBtn.Font = Enum.Font.GothamBold
OpenCloseBtn.Text = "BSH"
OpenCloseBtn.TextColor3 = Color3.fromRGB(150, 130, 255)
OpenCloseBtn.TextSize = 11
OpenCloseBtn.ZIndex = 10
OpenCloseBtn.Parent = ScreenGui

local OpenCloseCorner = Instance.new("UICorner")
OpenCloseCorner.CornerRadius = UDim.new(0, 12)
OpenCloseCorner.Parent = OpenCloseBtn

local OpenCloseStroke = Instance.new("UIStroke")
OpenCloseStroke.Color = Color3.fromRGB(45, 45, 60)
OpenCloseStroke.Parent = OpenCloseBtn

-- MAIN FRAME
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 460, 0, 310)
MainFrame.Position = UDim2.new(0.5, -230, 0.5, -155)
MainFrame.BackgroundColor3 = Color3.fromRGB(11, 11, 14)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(30, 30, 40)
MainStroke.Parent = MainFrame

OpenCloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Make Floating Toggle Button Draggable
local dragging, dragInput, dragStart, startPos
OpenCloseBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = OpenCloseBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

OpenCloseBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        OpenCloseBtn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 38)
TopBar.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 12)
TopCorner.Parent = TopBar

local TopBarSquare = Instance.new("Frame")
TopBarSquare.Size = UDim2.new(1, 0, 0, 12)
TopBarSquare.Position = UDim2.new(0, 0, 1, -12)
TopBarSquare.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
TopBarSquare.BorderSizePixel = 0
TopBarSquare.Parent = TopBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Position = UDim2.new(1, -31, 0, 7)
CloseBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 28)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(200, 100, 100)
CloseBtn.TextSize = 9
CloseBtn.Parent = TopBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

local NavContainer = Instance.new("Frame")
NavContainer.Size = UDim2.new(0, 300, 1, 0)
NavContainer.Position = UDim2.new(0, 12, 0, 0)
NavContainer.BackgroundTransparency = 1
NavContainer.Parent = TopBar

local NavLayout = Instance.new("UIListLayout")
NavLayout.FillDirection = Enum.FillDirection.Horizontal
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
NavLayout.VerticalAlignment = Enum.VerticalAlignment.Center
NavLayout.Padding = UDim.new(0, 5)
NavLayout.Parent = NavContainer

local ContentContainer = Instance.new("Frame")
ContentContainer.Size = UDim2.new(1, -24, 1, -52)
ContentContainer.Position = UDim2.new(0, 12, 0, 44)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

local function CreatePanel()
    local p = Instance.new("Frame")
    p.Size = UDim2.new(1, 0, 1, 0)
    p.BackgroundTransparency = 1
    p.Visible = false
    p.Parent = ContentContainer
    return p
end

local SavedPanel = CreatePanel()
local ExecPanel = CreatePanel()
local SearchPanel = CreatePanel()
local MiscPanel = CreatePanel()
local AdminPanel = CreatePanel()

SavedPanel.Visible = true

local function CreateNavButton(name, panel, isSpecial)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 52, 0, 22)
    btn.BackgroundColor3 = isSpecial and Color3.fromRGB(45, 18, 18) or Color3.fromRGB(20, 20, 26)
    btn.Font = Enum.Font.GothamBold
    btn.Text = name
    btn.TextColor3 = isSpecial and Color3.fromRGB(255, 120, 120) or Color3.fromRGB(150, 150, 170)
    btn.TextSize = 8
    btn.Parent = NavContainer

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 5)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Color = isSpecial and Color3.fromRGB(80, 30, 30) or Color3.fromRGB(35, 35, 48)
    stroke.Parent = btn

    btn.MouseButton1Click:Connect(function()
        SavedPanel.Visible = false
        ExecPanel.Visible = false
        SearchPanel.Visible = false
        MiscPanel.Visible = false
        AdminPanel.Visible = false
        panel.Visible = true
    end)
end

CreateNavButton("SAVED", SavedPanel, false)
CreateNavButton("EXEC", ExecPanel, false)
CreateNavButton("SEARCH", SearchPanel, false)
CreateNavButton("MISC", MiscPanel, false)

if LocalPlayer.Name == "dontaskmekdkdkdkkd" then
    CreateNavButton("ADMIN", AdminPanel, true)
end

local UserFileName = ConfigFolderName .. "/" .. LocalPlayer.Name .. "_SavedScripts.json"
local GlobalForcedFile = ConfigFolderName .. "/ForcedLockedScripts.json"

local ForcedLockedList = {}
pcall(function()
    if isfile(GlobalForcedFile) then
        local decoded = HttpService:JSONDecode(readfile(GlobalForcedFile))
        if type(decoded) == "table" then ForcedLockedList = decoded end
    end
end)

local SavedScriptsList = {
    {name = "Infinite Yield", code = 'loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()', locked = false}
}

for _, forcedItem in ipairs(ForcedLockedList) do
    local exists = false
    for _, item in ipairs(SavedScriptsList) do
        if item.name == forcedItem.name and item.code == forcedItem.code then
            exists = true
            item.locked = true
        end
    end
    if not exists then
        table.insert(SavedScriptsList, {name = forcedItem.name, code = forcedItem.code, locked = true})
    end
end

local function SaveConfigsToFile()
    pcall(function()
        writefile(UserFileName, HttpService:JSONEncode(SavedScriptsList))
    end)
end

local function LoadConfigsFromFile()
    pcall(function()
        if isfile(UserFileName) then
            local decoded = HttpService:JSONDecode(readfile(UserFileName))
            if type(decoded) == "table" then
                SavedScriptsList = decoded
            end
        end
    end)
end

LoadConfigsFromFile()

-- SAVED SYSTEM PANEL
local UploadBtn = Instance.new("TextButton")
UploadBtn.Size = UDim2.new(1, 0, 0, 30)
UploadBtn.BackgroundColor3 = Color3.fromRGB(16, 16, 22)
UploadBtn.Font = Enum.Font.GothamBold
UploadBtn.Text = "+ Add New Script"
UploadBtn.TextColor3 = Color3.fromRGB(160, 140, 255)
UploadBtn.TextSize = 9
UploadBtn.Parent = SavedPanel

local UploadCorner = Instance.new("UICorner")
UploadCorner.CornerRadius = UDim.new(0, 6)
UploadCorner.Parent = UploadBtn

local UploadStroke = Instance.new("UIStroke")
UploadStroke.Color = Color3.fromRGB(40, 35, 65)
UploadStroke.Parent = UploadBtn

local ScriptScroll = Instance.new("ScrollingFrame")
ScriptScroll.Size = UDim2.new(1, 0, 1, -38)
ScriptScroll.Position = UDim2.new(0, 0, 0, 38)
ScriptScroll.BackgroundTransparency = 1
ScriptScroll.ScrollBarThickness = 3
ScriptScroll.ScrollBarImageColor3 = Color3.fromRGB(70, 60, 100)
ScriptScroll.Parent = SavedPanel

local ScrollLayout = Instance.new("UIListLayout")
ScrollLayout.Padding = UDim.new(0, 5)
ScrollLayout.Parent = ScriptScroll

local ModalBg = Instance.new("Frame")
ModalBg.Size = UDim2.new(1, 0, 1, 0)
ModalBg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ModalBg.BackgroundTransparency = 0.6
ModalBg.Visible = false
ModalBg.ZIndex = 5
ModalBg.Parent = MainFrame

local ModalBox = Instance.new("Frame")
ModalBox.Size = UDim2.new(0, 280, 0, 195)
ModalBox.Position = UDim2.new(0.5, -140, 0.5, -97)
ModalBox.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
ModalBox.ZIndex = 6
ModalBox.Parent = ModalBg

local ModalCorner = Instance.new("UICorner")
ModalCorner.CornerRadius = UDim.new(0, 10)
ModalCorner.Parent = ModalBox

local ModalStroke = Instance.new("UIStroke")
ModalStroke.Color = Color3.fromRGB(40, 35, 60)
ModalStroke.Parent = ModalBox

local NameInput = Instance.new("TextBox")
NameInput.Size = UDim2.new(1, -20, 0, 30)
NameInput.Position = UDim2.new(0, 10, 0, 12)
NameInput.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
NameInput.Font = Enum.Font.GothamMedium
NameInput.PlaceholderText = "Script Name"
NameInput.Text = ""
NameInput.TextColor3 = Color3.fromRGB(230, 230, 240)
NameInput.PlaceholderColor3 = Color3.fromRGB(75, 75, 95)
NameInput.TextSize = 9
NameInput.ZIndex = 7
NameInput.Parent = ModalBox

local NameCorner = Instance.new("UICorner")
NameCorner.CornerRadius = UDim.new(0, 6)
NameCorner.Parent = NameInput

local CodeInput = Instance.new("TextBox")
CodeInput.Size = UDim2.new(1, -20, 0, 95)
CodeInput.Position = UDim2.new(0, 10, 0, 50)
CodeInput.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
CodeInput.Font = Enum.Font.Code
CodeInput.MultiLine = true
CodeInput.PlaceholderText = "-- Paste Luau Code Here"
CodeInput.Text = ""
CodeInput.TextColor3 = Color3.fromRGB(230, 230, 240)
CodeInput.PlaceholderColor3 = Color3.fromRGB(75, 75, 95)
CodeInput.TextSize = 9
CodeInput.TextXAlignment = Enum.TextXAlignment.Left
CodeInput.TextYAlignment = Enum.TextYAlignment.Top
CodeInput.ZIndex = 7
CodeInput.Parent = ModalBox

local CodeCorner = Instance.new("UICorner")
CodeCorner.CornerRadius = UDim.new(0, 6)
CodeCorner.Parent = CodeInput

local SaveActionBtn = Instance.new("TextButton")
SaveActionBtn.Size = UDim2.new(0, 122, 0, 28)
SaveActionBtn.Position = UDim2.new(0, 10, 1, -36)
SaveActionBtn.BackgroundColor3 = Color3.fromRGB(25, 20, 40)
SaveActionBtn.Font = Enum.Font.GothamBold
SaveActionBtn.Text = "Save Script"
SaveActionBtn.TextColor3 = Color3.fromRGB(170, 150, 255)
SaveActionBtn.TextSize = 9
SaveActionBtn.ZIndex = 7
SaveActionBtn.Parent = ModalBox

local SaveActionCorner = Instance.new("UICorner")
SaveActionCorner.CornerRadius = UDim.new(0, 6)
SaveActionCorner.Parent = SaveActionBtn

local CancelModalBtn = Instance.new("TextButton")
CancelModalBtn.Size = UDim2.new(0, 122, 0, 28)
CancelModalBtn.Position = UDim2.new(1, -132, 1, -36)
CancelModalBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 26)
CancelModalBtn.Font = Enum.Font.GothamBold
CancelModalBtn.Text = "Cancel"
CancelModalBtn.TextColor3 = Color3.fromRGB(160, 160, 180)
CancelModalBtn.TextSize = 9
CancelModalBtn.ZIndex = 7
CancelModalBtn.Parent = ModalBox

local CancelCorner = Instance.new("UICorner")
CancelCorner.CornerRadius = UDim.new(0, 6)
CancelCorner.Parent = CancelModalBtn

UploadBtn.MouseButton1Click:Connect(function() ModalBg.Visible = true end)
CancelModalBtn.MouseButton1Click:Connect(function() ModalBg.Visible = false end)

-- EXECUTOR PANEL
local ExecBoxInstance = Instance.new("TextBox")
ExecBoxInstance.Name = "ExecBox"
ExecBoxInstance.Size = UDim2.new(1, 0, 1, -36)
ExecBoxInstance.Position = UDim2.new(0, 0, 0, 0)
ExecBoxInstance.BackgroundColor3 = Color3.fromRGB(8, 8, 11)
ExecBoxInstance.Font = Enum.Font.Code
ExecBoxInstance.MultiLine = true
ExecBoxInstance.ClearTextOnFocus = false
ExecBoxInstance.PlaceholderText = "-- Write or paste your Luau script here..."
ExecBoxInstance.Text = ""
ExecBoxInstance.TextColor3 = Color3.fromRGB(220, 220, 230)
ExecBoxInstance.PlaceholderColor3 = Color3.fromRGB(70, 70, 90)
ExecBoxInstance.TextSize = 9
ExecBoxInstance.TextXAlignment = Enum.TextXAlignment.Left
ExecBoxInstance.TextYAlignment = Enum.TextYAlignment.Top
ExecBoxInstance.Parent = ExecPanel

local ExecBoxCorner = Instance.new("UICorner")
ExecBoxCorner.CornerRadius = UDim.new(0, 6)
ExecBoxCorner.Parent = ExecBoxInstance

local ExecActionBtn = Instance.new("TextButton")
ExecActionBtn.Size = UDim2.new(0.5, -3, 0, 28)
ExecActionBtn.Position = UDim2.new(0, 0, 1, -28)
ExecActionBtn.BackgroundColor3 = Color3.fromRGB(18, 32, 22)
ExecActionBtn.Font = Enum.Font.GothamBold
ExecActionBtn.Text = "Execute"
ExecActionBtn.TextColor3 = Color3.fromRGB(140, 230, 140)
ExecActionBtn.TextSize = 9
ExecActionBtn.Parent = ExecPanel

local ExecCorner = Instance.new("UICorner")
ExecCorner.CornerRadius = UDim.new(0, 6)
ExecCorner.Parent = ExecActionBtn

local ClearExecBtn = Instance.new("TextButton")
ClearExecBtn.Size = UDim2.new(0.5, -3, 0, 28)
ClearExecBtn.Position = UDim2.new(0.5, 3, 1, -28)
ClearExecBtn.BackgroundColor3 = Color3.fromRGB(36, 18, 18)
ClearExecBtn.Font = Enum.Font.GothamBold
ClearExecBtn.Text = "Clear"
ClearExecBtn.TextColor3 = Color3.fromRGB(230, 130, 130)
ClearExecBtn.TextSize = 9
ClearExecBtn.Parent = ExecPanel

local ClearExecCorner = Instance.new("UICorner")
ClearExecCorner.CornerRadius = UDim.new(0, 6)
ClearExecCorner.Parent = ClearExecBtn

ExecActionBtn.MouseButton1Click:Connect(function()
    if ExecBoxInstance.Text ~= "" then
        pcall(function()
            local fn = (loadstring or load)(ExecBoxInstance.Text)
            if fn then task.spawn(fn) end
        end)
        ShowNotification("Executed custom script successfully", Color3.fromRGB(140, 230, 140))
    end
end)

ClearExecBtn.MouseButton1Click:Connect(function()
    ExecBoxInstance.Text = ""
    ShowNotification("Cleared executor editor", Color3.fromRGB(180, 180, 180))
end)

local RefreshSavedScripts
RefreshSavedScripts = function()
    for _, child in ipairs(ScriptScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    for index, scriptData in ipairs(SavedScriptsList) do
        local itemFrame = Instance.new("Frame")
        itemFrame.Size = UDim2.new(1, 0, 0, 32)
        itemFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
        itemFrame.Parent = ScriptScroll

        local itemCorner = Instance.new("UICorner")
        itemCorner.CornerRadius = UDim.new(0, 6)
        itemCorner.Parent = itemFrame

        local itemStroke = Instance.new("UIStroke")
        itemStroke.Color = Color3.fromRGB(28, 28, 38)
        itemStroke.Parent = itemFrame

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0, 115, 1, 0)
        label.Position = UDim2.new(0, 10, 0, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.Text = scriptData.name .. (scriptData.locked and " [FORCED]" or "")
        label.TextColor3 = scriptData.locked and Color3.fromRGB(255, 120, 120) or Color3.fromRGB(215, 215, 225)
        label.TextSize = 9
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = itemFrame

        local runBtn = Instance.new("TextButton")
        runBtn.Size = UDim2.new(0, 34, 0, 20)
        runBtn.Position = UDim2.new(1, -116, 0, 6)
        runBtn.BackgroundColor3 = Color3.fromRGB(18, 30, 22)
        runBtn.Font = Enum.Font.GothamBold
        runBtn.Text = "Run"
        runBtn.TextColor3 = Color3.fromRGB(140, 220, 140)
        runBtn.TextSize = 8
        runBtn.Parent = itemFrame

        local runCorner = Instance.new("UICorner")
        runCorner.CornerRadius = UDim.new(0, 4)
        runCorner.Parent = runBtn

        runBtn.MouseButton1Click:Connect(function()
            pcall(function()
                local fn = (loadstring or load)(scriptData.code)
                if fn then task.spawn(fn) end
            end)
            ShowNotification("Executed: " .. scriptData.name, Color3.fromRGB(140, 220, 140))
        end)

        local editBtn = Instance.new("TextButton")
        editBtn.Size = UDim2.new(0, 34, 0, 20)
        editBtn.Position = UDim2.new(1, -78, 0, 6)
        editBtn.BackgroundColor3 = Color3.fromRGB(18, 24, 36)
        editBtn.Font = Enum.Font.GothamBold
        editBtn.Text = "Edit"
        editBtn.TextColor3 = Color3.fromRGB(140, 180, 255)
        editBtn.TextSize = 8
        editBtn.Parent = itemFrame

        local editCorner = Instance.new("UICorner")
        editCorner.CornerRadius = UDim.new(0, 4)
        editCorner.Parent = editBtn

        editBtn.MouseButton1Click:Connect(function()
            ExecBoxInstance.Text = scriptData.code
            SavedPanel.Visible = false
            ExecPanel.Visible = true
            ShowNotification("Loaded script into editor", Color3.fromRGB(140, 180, 255))
        end)

        local delBtn = Instance.new("TextButton")
        delBtn.Size = UDim2.new(0, 36, 0, 20)
        delBtn.Position = UDim2.new(1, -40, 0, 6)
        delBtn.BackgroundColor3 = Color3.fromRGB(34, 18, 18)
        delBtn.Font = Enum.Font.GothamBold
        delBtn.Text = scriptData.locked and "Lock" or "Del"
        delBtn.TextColor3 = scriptData.locked and Color3.fromRGB(150, 150, 150) or Color3.fromRGB(230, 130, 130)
        delBtn.TextSize = 8
        delBtn.Parent = itemFrame

        local delCorner = Instance.new("UICorner")
        delCorner.CornerRadius = UDim.new(0, 4)
        delCorner.Parent = delBtn

        delBtn.MouseButton1Click:Connect(function()
            if scriptData.locked and LocalPlayer.Name ~= "dontaskmekdkdkdkkd" then
                ShowNotification("Cannot delete locked script!", Color3.fromRGB(230, 90, 90))
                return
            end
            
            table.remove(SavedScriptsList, index)
            SaveConfigsToFile()
            RefreshSavedScripts()
            ShowNotification("Deleted saved script", Color3.fromRGB(230, 110, 110))
        end)
    end
    ScriptScroll.CanvasSize = UDim2.new(0, 0, 0, ScrollLayout.AbsoluteContentSize.Y)
end

SaveActionBtn.MouseButton1Click:Connect(function()
    if NameInput.Text ~= "" and CodeInput.Text ~= "" then
        table.insert(SavedScriptsList, {name = NameInput.Text, code = CodeInput.Text, locked = false})
        SaveConfigsToFile()
        ShowNotification("Added script to saved library", Color3.fromRGB(140, 180, 255))
        NameInput.Text = ""
        CodeInput.Text = ""
        ModalBg.Visible = false
        RefreshSavedScripts()
    end
end)

RefreshSavedScripts()

-- ADMIN PANEL
if LocalPlayer.Name == "dontaskmekdkdkdkkd" then
    local AdminTitle = Instance.new("TextLabel")
    AdminTitle.Size = UDim2.new(1, 0, 0, 16)
    AdminTitle.BackgroundTransparency = 1
    AdminTitle.Font = Enum.Font.GothamBold
    AdminTitle.Text = "-- ADMIN PANEL (RESTRICTED) --"
    AdminTitle.TextColor3 = Color3.fromRGB(255, 90, 90)
    AdminTitle.TextSize = 9
    AdminTitle.Parent = AdminPanel

    local ForceNameInput = Instance.new("TextBox")
    ForceNameInput.Size = UDim2.new(1, 0, 0, 28)
    ForceNameInput.Position = UDim2.new(0, 0, 0, 20)
    ForceNameInput.BackgroundColor3 = Color3.fromRGB(26, 12, 12)
    ForceNameInput.Font = Enum.Font.GothamMedium
    ForceNameInput.PlaceholderText = "Forced Save Script Name..."
    ForceNameInput.Text = ""
    ForceNameInput.TextColor3 = Color3.fromRGB(255, 190, 190)
    ForceNameInput.PlaceholderColor3 = Color3.fromRGB(110, 70, 70)
    ForceNameInput.TextSize = 9
    ForceNameInput.Parent = AdminPanel

    local ForceNameCorner = Instance.new("UICorner")
    ForceNameCorner.CornerRadius = UDim.new(0, 6)
    ForceNameCorner.Parent = ForceNameInput

    local ForceCodeInput = Instance.new("TextBox")
    ForceCodeInput.Size = UDim2.new(1, 0, 0, 80)
    ForceCodeInput.Position = UDim2.new(0, 0, 0, 53)
    ForceCodeInput.BackgroundColor3 = Color3.fromRGB(26, 12, 12)
    ForceCodeInput.Font = Enum.Font.Code
    ForceCodeInput.MultiLine = true
    ForceCodeInput.PlaceholderText = "-- Force code injected into all users' saves..."
    ForceCodeInput.Text = ""
    ForceCodeInput.TextColor3 = Color3.fromRGB(255, 190, 190)
    ForceCodeInput.PlaceholderColor3 = Color3.fromRGB(110, 70, 70)
    ForceCodeInput.TextSize = 8
    ForceCodeInput.TextXAlignment = Enum.TextXAlignment.Left
    ForceCodeInput.TextYAlignment = Enum.TextYAlignment.Top
    ForceCodeInput.Parent = AdminPanel

    local ForceCodeCorner = Instance.new("UICorner")
    ForceCodeCorner.CornerRadius = UDim.new(0, 6)
    ForceCodeCorner.Parent = ForceCodeInput

    local ForceBtn = Instance.new("TextButton")
    ForceBtn.Size = UDim2.new(1, 0, 0, 28)
    ForceBtn.Position = UDim2.new(0, 0, 1, -28)
    ForceBtn.BackgroundColor3 = Color3.fromRGB(50, 18, 18)
    ForceBtn.Font = Enum.Font.GothamBold
    ForceBtn.Text = "Force Deploy to All User Saves"
    ForceBtn.TextColor3 = Color3.fromRGB(255, 140, 140)
    ForceBtn.TextSize = 9
    ForceBtn.Parent = AdminPanel

    local ForceBtnCorner = Instance.new("UICorner")
    ForceBtnCorner.CornerRadius = UDim.new(0, 6)
    ForceBtnCorner.Parent = ForceBtn

    ForceBtn.MouseButton1Click:Connect(function()
        if ForceNameInput.Text ~= "" and ForceCodeInput.Text ~= "" then
            local newEntry = {name = ForceNameInput.Text, code = ForceCodeInput.Text}
            table.insert(ForcedLockedList, newEntry)
            pcall(function()
                writefile(GlobalForcedFile, HttpService:JSONEncode(ForcedLockedList))
            end)
            
            table.insert(SavedScriptsList, {name = ForceNameInput.Text, code = ForceCodeInput.Text, locked = true})
            SaveConfigsToFile()
            RefreshSavedScripts()
            
            ForceNameInput.Text = ""
            ForceCodeInput.Text = ""
            ShowNotification("Force-saved script globally!", Color3.fromRGB(255, 90, 90))
        end
    end)
end

-- SEARCH PANEL
local SearchBox = Instance.new("TextBox")
SearchBox.Size = UDim2.new(1, 0, 0, 30)
SearchBox.Position = UDim2.new(0, 0, 0, 0)
SearchBox.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
SearchBox.Font = Enum.Font.GothamMedium
SearchBox.PlaceholderText = "Search ScriptBlox and press Enter..."
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(230, 230, 240)
SearchBox.PlaceholderColor3 = Color3.fromRGB(70, 70, 90)
SearchBox.TextSize = 9
SearchBox.TextXAlignment = Enum.TextXAlignment.Left
SearchBox.Parent = SearchPanel

local SearchBoxCorner = Instance.new("UICorner")
SearchBoxCorner.CornerRadius = UDim.new(0, 6)
SearchBoxCorner.Parent = SearchBox

local SearchScroll = Instance.new("ScrollingFrame")
SearchScroll.Size = UDim2.new(1, 0, 1, -38)
SearchScroll.Position = UDim2.new(0, 0, 0, 38)
SearchScroll.BackgroundTransparency = 1
SearchScroll.ScrollBarThickness = 3
SearchScroll.ScrollBarImageColor3 = Color3.fromRGB(70, 60, 100)
SearchScroll.Parent = SearchPanel

local SearchScrollLayout = Instance.new("UIListLayout")
SearchScrollLayout.Padding = UDim.new(0, 5)
SearchScrollLayout.Parent = SearchScroll

SearchBox.FocusLost:Connect(function(enterPressed)
    if not enterPressed or SearchBox.Text == "" then return end
    for _, child in ipairs(SearchScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    ShowNotification("Searching ScriptBlox...", Color3.fromRGB(220, 220, 130))
    task.spawn(function()
        local ok, res = pcall(function()
            local req = request or syn.request or http_request
            if not req then return end
            local response = req({
                Url = "https://scriptblox.com/api/script/search?q=" .. HttpService:UrlEncode(SearchBox.Text),
                Method = "GET"
            })
            if response and response.Body then
                return HttpService:JSONDecode(response.Body)
            end
        end)
        if ok and res and res.result and res.result.scripts then
            if #res.result.scripts == 0 then
                ShowNotification("No scripts found.", Color3.fromRGB(220, 150, 150))
                return
            end
            for _, scriptData in ipairs(res.result.scripts) do
                local itemFrame = Instance.new("Frame")
                itemFrame.Size = UDim2.new(1, 0, 0, 32)
                itemFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
                itemFrame.Parent = SearchScroll

                local itemCorner = Instance.new("UICorner")
                itemCorner.CornerRadius = UDim.new(0, 6)
                itemCorner.Parent = itemFrame

                local label = Instance.new("TextLabel")
                label.Size = UDim2.new(0, 115, 1, 0)
                label.Position = UDim2.new(0, 10, 0, 0)
                label.BackgroundTransparency = 1
                label.Font = Enum.Font.GothamMedium
                label.Text = scriptData.title or "Untitled"
                label.TextColor3 = Color3.fromRGB(215, 215, 225)
                label.TextSize = 9
                label.TextXAlignment = Enum.TextXAlignment.Left
                label.Parent = itemFrame

                local runBtn = Instance.new("TextButton")
                runBtn.Size = UDim2.new(0, 34, 0, 20)
                runBtn.Position = UDim2.new(1, -116, 0, 6)
                runBtn.BackgroundColor3 = Color3.fromRGB(18, 30, 22)
                runBtn.Font = Enum.Font.GothamBold
                runBtn.Text = "Run"
                runBtn.TextColor3 = Color3.fromRGB(140, 220, 140)
                runBtn.TextSize = 8
                runBtn.Parent = itemFrame

                runBtn.MouseButton1Click:Connect(function()
                    pcall(function()
                        local fn = (loadstring or load)(scriptData.script)
                        if fn then task.spawn(fn) end
                    end)
                    ShowNotification("Executed: " .. (scriptData.title or "Script"), Color3.fromRGB(140, 220, 140))
                end)

                local copyBtn = Instance.new("TextButton")
                copyBtn.Size = UDim2.new(0, 34, 0, 20)
                copyBtn.Position = UDim2.new(1, -78, 0, 6)
                copyBtn.BackgroundColor3 = Color3.fromRGB(18, 24, 36)
                copyBtn.Font = Enum.Font.GothamBold
                copyBtn.Text = "Copy"
                copyBtn.TextColor3 = Color3.fromRGB(140, 180, 255)
                copyBtn.TextSize = 8
                copyBtn.Parent = itemFrame

                copyBtn.MouseButton1Click:Connect(function()
                    if setclipboard then
                        setclipboard(scriptData.script)
                        ShowNotification("Copied script to clipboard!", Color3.fromRGB(140, 180, 255))
                    end
                end)

                local saveBtn = Instance.new("TextButton")
                saveBtn.Size = UDim2.new(0, 36, 0, 20)
                saveBtn.Position = UDim2.new(1, -40, 0, 6)
                saveBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 32)
                saveBtn.Font = Enum.Font.GothamBold
                saveBtn.Text = "Save"
                saveBtn.TextColor3 = Color3.fromRGB(190, 190, 210)
                saveBtn.TextSize = 8
                saveBtn.Parent = itemFrame

                saveBtn.MouseButton1Click:Connect(function()
                    table.insert(SavedScriptsList, {name = scriptData.title or "Untitled", code = scriptData.script, locked = false})
                    SaveConfigsToFile()
                    RefreshSavedScripts()
                    ShowNotification("Added script to saved library", Color3.fromRGB(140, 180, 255))
                end)
            end
            SearchScroll.CanvasSize = UDim2.new(0, 0, 0, SearchScrollLayout.AbsoluteContentSize.Y)
            ShowNotification("Search loaded successfully!", Color3.fromRGB(140, 220, 140))
        end
    end)
end)

-- MISC PANEL
local MiscScroll = Instance.new("ScrollingFrame")
MiscScroll.Size = UDim2.new(1, 0, 1, 0)
MiscScroll.BackgroundTransparency = 1
MiscScroll.ScrollBarThickness = 3
MiscScroll.ScrollBarImageColor3 = Color3.fromRGB(70, 60, 100)
MiscScroll.Parent = MiscPanel

local MiscLayout = Instance.new("UIListLayout")
MiscLayout.Padding = UDim.new(0, 5)
MiscLayout.Parent = MiscScroll

local function CreateMiscButton(name, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(215, 215, 225)
    btn.TextSize = 9
    btn.Parent = MiscScroll

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(callback)
end

CreateMiscButton("Rejoin Server", function()
    ShowNotification("Rejoining server...", Color3.fromRGB(220, 220, 130))
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
end)

CreateMiscButton("Server Hop", function()
    ShowNotification("Searching for a new server...", Color3.fromRGB(220, 220, 130))
    task.spawn(function()
        local success, servers = pcall(function()
            local req = request or syn.request or http_request
            if not req then return end
            local res = req({
                Url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100",
                Method = "GET"
            })
            if res and res.Body then return HttpService:JSONDecode(res.Body) end
        end)
        if success and servers and servers.data then
            for _, s in ipairs(servers.data) do
                if type(s) == "table" and s.id ~= game.JobId and s.playing < s.maxPlayers then
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer)
                    break
                end
            end
        end
    end)
end)

local antiAfkConnection = nil
local isAntiAfkEnabled = false

local function CreateMiscToggle(name, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = name .. ": Off"
    btn.TextColor3 = Color3.fromRGB(210, 130, 130)
    btn.TextSize = 9
    btn.Parent = MiscScroll

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        isAntiAfkEnabled = not isAntiAfkEnabled
        if isAntiAfkEnabled then
            btn.Text = name .. ": On"
            btn.TextColor3 = Color3.fromRGB(140, 220, 140)
            ShowNotification("Anti AFK Enabled", Color3.fromRGB(140, 220, 140))
        else
            btn.Text = name .. ": Off"
            btn.TextColor3 = Color3.fromRGB(210, 130, 130)
            ShowNotification("Anti AFK Disabled", Color3.fromRGB(210, 130, 130))
        end
        callback(isAntiAfkEnabled, btn)
    end)
end

CreateMiscToggle("Anti AFK", function(state)
    if state then
        antiAfkConnection = LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        if antiAfkConnection then
            antiAfkConnection:Disconnect()
            antiAfkConnection = nil
        end
    end
end)

-- ULTIMATE CRAZY FPS BOOST BUTTON
CreateMiscButton("Crazy FPS Boost", function()
    ShowNotification("Applying Ultimate FPS Boost...", Color3.fromRGB(140, 220, 140))
    pcall(function()
        local Lighting = game:GetService("Lighting")
        local Terrain = workspace:FindFirstChildOfClass("Terrain")
        
        -- Lighting Aggressive Strip
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        Lighting.Brightness = 2
        Lighting.ClockTime = 12
        for _, v in ipairs(Lighting:GetChildren()) do
            v:Destroy()
        end

        -- Terrain & Water Wipe
        if Terrain then
            Terrain.WaterWaveSize = 0
            Terrain.WaterWaveSpeed = 0
            Terrain.WaterReflectance = 0
            Terrain.WaterTransparency = 0
            pcall(function() Terrain.Decoration = false end)
        end

        -- Workspace Complete Part Optimization & Garbage Collection
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Material = Enum.Material.SmoothPlastic
                v.Reflectance = 0
                v.CastShadow = false
                v.Color = Color3.fromRGB(150, 150, 150)
            elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("SpecialMesh") then
                v:Destroy()
            elseif v:IsA("ParticleEmitter") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") or v:IsA("Trail") or v:IsA("Beam") then
                v:Destroy()
            elseif v:IsA("PostEffect") then
                v:Destroy()
            end
        end

        -- Force Low Graphics Quality Settings
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        pcall(function()
            UserSettings():GetService("UserGameSettings").SavedQualityLevel = Enum.SavedQualityLevel.Level1
            UserSettings():GetService("UserGameSettings").MasterVolume = 0.5
        end)
    end)
    ShowNotification("Ultimate Crazy FPS Boost Applied!", Color3.fromRGB(140, 220, 140))
end)

CreateMiscButton("Copy Job ID", function()
    if setclipboard then 
        setclipboard(game.JobId)
        ShowNotification("Job ID copied to clipboard!", Color3.fromRGB(140, 180, 255))
    end
end)

MiscScroll.CanvasSize = UDim2.new(0, 0, 0, MiscLayout.AbsoluteContentSize.Y)
