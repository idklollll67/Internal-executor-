-- BSH Hub (Fixed Executor Menu Layout & Content Clipping)

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local VirtualUser = game:GetService("VirtualUser")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local ConfigFolderName = "BSHHub_Configs"

pcall(function()
    if not isfolder(ConfigFolderName) then makefolder(ConfigFolderName) end
end)

local ParentGui = (gethui and gethui()) or LocalPlayer:WaitForChild("PlayerGui")

if ParentGui:FindFirstChild("BSHInternalHub") then
    ParentGui.BSHInternalHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BSHInternalHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = ParentGui

-- NOTIFICATION SYSTEM
local NotifContainer = Instance.new("Frame")
NotifContainer.Size = UDim2.new(0, 220, 0, 250)
NotifContainer.Position = UDim2.new(1, -230, 1, -260)
NotifContainer.BackgroundTransparency = 1
NotifContainer.Parent = ScreenGui

local NotifLayout = Instance.new("UIListLayout")
NotifLayout.SortOrder = Enum.SortOrder.LayoutOrder
NotifLayout.VerticalAlignment = Enum.VerticalAlignment.Bottom
NotifLayout.Padding = UDim.new(0, 6)
NotifLayout.Parent = NotifContainer

local function ShowNotification(message, color)
    task.spawn(function()
        local notif = Instance.new("Frame")
        notif.Size = UDim2.new(1, 0, 0, 32)
        notif.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        notif.BackgroundTransparency = 1
        notif.Parent = NotifContainer

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 4)
        corner.Parent = notif

        local indicator = Instance.new("Frame")
        indicator.Size = UDim2.new(0, 3, 1, 0)
        indicator.BackgroundColor3 = color or Color3.fromRGB(180, 180, 210)
        indicator.BorderSizePixel = 0
        indicator.Parent = notif

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -12, 1, 0)
        label.Position = UDim2.new(0, 10, 0, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.Text = message
        label.TextColor3 = Color3.fromRGB(230, 230, 240)
        label.TextSize = 9
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = notif

        local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
        game:GetService("TweenService"):Create(notif, tweenInfo, {BackgroundTransparency = 0.1}):Play()

        task.wait(2.5)

        local fadeOut = game:GetService("TweenService"):Create(notif, tweenInfo, {BackgroundTransparency = 1})
        fadeOut:Play()
        fadeOut.Completed:Connect(function()
            notif:Destroy()
        end)
    end)
end

-- OPEN/CLOSE FLOATING X TOGGLE BUTTON (DRAGGABLE)
local OpenCloseBtn = Instance.new("TextButton")
OpenCloseBtn.Size = UDim2.new(0, 36, 0, 36)
OpenCloseBtn.Position = UDim2.new(0, 12, 0.5, -18)
OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
OpenCloseBtn.Font = Enum.Font.GothamBold
OpenCloseBtn.Text = "X"
OpenCloseBtn.TextColor3 = Color3.fromRGB(220, 160, 160)
OpenCloseBtn.TextSize = 12
OpenCloseBtn.ZIndex = 10
OpenCloseBtn.Parent = ScreenGui

local OpenCloseCorner = Instance.new("UICorner")
OpenCloseCorner.CornerRadius = UDim.new(0, 6)
OpenCloseCorner.Parent = OpenCloseBtn

-- MAIN FRAME
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 440, 0, 300)
MainFrame.Position = UDim2.new(0.5, -220, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

OpenCloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Make Floating Toggle Button Draggable for Mobile/PC
local dragging, dragInput, dragStart, startPos
OpenCloseBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = OpenCloseBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

OpenCloseBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        OpenCloseBtn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 6)
MainCorner.Parent = MainFrame

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 34)
TopBar.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
TopBar.BackgroundTransparency = 0.3
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 6)
TopCorner.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(0, 120, 1, 0)
TitleLabel.Position = UDim2.new(0, 12, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = ""
TitleLabel.TextColor3 = Color3.fromRGB(240, 240, 245)
TitleLabel.TextSize = 12
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Position = UDim2.new(1, -30, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(220, 160, 160)
CloseBtn.TextSize = 9
CloseBtn.Parent = TopBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 4)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

local NavContainer = Instance.new("Frame")
NavContainer.Size = UDim2.new(0, 280, 1, 0)
NavContainer.Position = UDim2.new(0, 80, 0, 0)
NavContainer.BackgroundTransparency = 1
NavContainer.Parent = TopBar

local NavLayout = Instance.new("UIListLayout")
NavLayout.FillDirection = Enum.FillDirection.Horizontal
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
NavLayout.VerticalAlignment = Enum.VerticalAlignment.Center
NavLayout.Padding = UDim.new(0, 4)
NavLayout.Parent = NavContainer

local ContentContainer = Instance.new("Frame")
ContentContainer.Size = UDim2.new(1, -16, 1, -44)
ContentContainer.Position = UDim2.new(0, 8, 0, 38)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

local function CreatePanel()
    local p = Instance.new("Frame")
    p.Size = UDim2.new(1, 0, 1, 0)
    p.BackgroundTransparency = 1
    p.Visible = false
    p.Parent = ContentContainer
    return p
end

local SavedPanel = CreatePanel()
local ExecPanel = CreatePanel()
local SearchPanel = CreatePanel()
local MiscPanel = CreatePanel()
local AdminPanel = CreatePanel()

SavedPanel.Visible = true

local function CreateNavButton(name, panel, isSpecial)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 42, 0, 22)
    btn.BackgroundColor3 = isSpecial and Color3.fromRGB(60, 15, 15) or Color3.fromRGB(28, 28, 35)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = name
    btn.TextColor3 = isSpecial and Color3.fromRGB(255, 120, 120) or Color3.fromRGB(160, 160, 175)
    btn.TextSize = 8
    btn.Parent = NavContainer

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 4)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        SavedPanel.Visible = false
        ExecPanel.Visible = false
        SearchPanel.Visible = false
        MiscPanel.Visible = false
        AdminPanel.Visible = false
        panel.Visible = true
    end)
end

CreateNavButton("SAVED", SavedPanel, false)
CreateNavButton("EXEC", ExecPanel, false)
CreateNavButton("SEARCH", SearchPanel, false)
CreateNavButton("MISC", MiscPanel, false)

if LocalPlayer.Name == "dontaskmekdkdkdkkd" then
    CreateNavButton("ADMIN", AdminPanel, true)
end

local UserFileName = ConfigFolderName .. "/" .. LocalPlayer.Name .. "_SavedScripts.json"
local GlobalForcedFile = ConfigFolderName .. "/ForcedLockedScripts.json"

local ForcedLockedList = {}
pcall(function()
    if isfile(GlobalForcedFile) then
        local decoded = HttpService:JSONDecode(readfile(GlobalForcedFile))
        if type(decoded) == "table" then ForcedLockedList = decoded end
    end
end)

local SavedScriptsList = {
    {name = "Infinite Yield", code = 'loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()', locked = false}
}

for _, forcedItem in ipairs(ForcedLockedList) do
    local exists = false
    for _, item in ipairs(SavedScriptsList) do
        if item.name == forcedItem.name and item.code == forcedItem.code then
            exists = true
            item.locked = true
        end
    end
    if not exists then
        table.insert(SavedScriptsList, {name = forcedItem.name, code = forcedItem.code, locked = true})
    end
end

local function SaveConfigsToFile()
    pcall(function()
        writefile(UserFileName, HttpService:JSONEncode(SavedScriptsList))
    end)
end

local function LoadConfigsFromFile()
    pcall(function()
        if isfile(UserFileName) then
            local decoded = HttpService:JSONDecode(readfile(UserFileName))
            if type(decoded) == "table" then
                SavedScriptsList = decoded
            end
        end
    end)
end

LoadConfigsFromFile()

for _, forcedItem in ipairs(ForcedLockedList) do
    local found = false
    for _, item in ipairs(SavedScriptsList) do
        if item.name == forcedItem.name then
            found = true
            item.locked = true
        end
    end
    if not found then
        table.insert(SavedScriptsList, {name = forcedItem.name, code = forcedItem.code, locked = true})
    end
end

-- SAVED SYSTEM PANEL
local UploadBtn = Instance.new("TextButton")
UploadBtn.Size = UDim2.new(1, 0, 0, 26)
UploadBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 28)
UploadBtn.Font = Enum.Font.GothamMedium
UploadBtn.Text = "Upload"
UploadBtn.TextColor3 = Color3.fromRGB(180, 180, 195)
UploadBtn.TextSize = 9
UploadBtn.Parent = SavedPanel

local UploadCorner = Instance.new("UICorner")
UploadCorner.CornerRadius = UDim.new(0, 4)
UploadCorner.Parent = UploadBtn

local ScriptScroll = Instance.new("ScrollingFrame")
ScriptScroll.Size = UDim2.new(1, 0, 1, -30)
ScriptScroll.Position = UDim2.new(0, 0, 0, 30)
ScriptScroll.BackgroundTransparency = 1
ScriptScroll.ScrollBarThickness = 2
ScriptScroll.Parent = SavedPanel

local ScrollLayout = Instance.new("UIListLayout")
ScrollLayout.Padding = UDim.new(0, 4)
ScrollLayout.Parent = ScriptScroll

local ModalBg = Instance.new("Frame")
ModalBg.Size = UDim2.new(1, 0, 1, 0)
ModalBg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ModalBg.BackgroundTransparency = 0.6
ModalBg.Visible = false
ModalBg.ZIndex = 5
ModalBg.Parent = MainFrame

local ModalBox = Instance.new("Frame")
ModalBox.Size = UDim2.new(0, 260, 0, 180)
ModalBox.Position = UDim2.new(0.5, -130, 0.5, -90)
ModalBox.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
ModalBox.ZIndex = 6
ModalBox.Parent = ModalBg

local ModalCorner = Instance.new("UICorner")
ModalCorner.CornerRadius = UDim.new(0, 6)
ModalCorner.Parent = ModalBox

local NameInput = Instance.new("TextBox")
NameInput.Size = UDim2.new(1, -16, 0, 26)
NameInput.Position = UDim2.new(0, 8, 0, 10)
NameInput.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
NameInput.Font = Enum.Font.Gotham
NameInput.PlaceholderText = "Script Name"
NameInput.Text = ""
NameInput.TextColor3 = Color3.fromRGB(240, 240, 245)
NameInput.TextSize = 9
NameInput.ZIndex = 7
NameInput.Parent = ModalBox

local NameCorner = Instance.new("UICorner")
NameCorner.CornerRadius = UDim.new(0, 4)
NameCorner.Parent = NameInput

local CodeInput = Instance.new("TextBox")
CodeInput.Size = UDim2.new(1, -16, 0, 90)
CodeInput.Position = UDim2.new(0, 8, 0, 42)
CodeInput.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
CodeInput.Font = Enum.Font.Code
CodeInput.MultiLine = true
CodeInput.PlaceholderText = "-- Paste Luau Code Here"
CodeInput.Text = ""
CodeInput.TextColor3 = Color3.fromRGB(240, 240, 245)
CodeInput.TextSize = 8
CodeInput.TextXAlignment = Enum.TextXAlignment.Left
CodeInput.TextYAlignment = Enum.TextYAlignment.Top
CodeInput.ZIndex = 7
CodeInput.Parent = ModalBox

local CodeCorner = Instance.new("UICorner")
CodeCorner.CornerRadius = UDim.new(0, 4)
CodeCorner.Parent = CodeInput

local SaveActionBtn = Instance.new("TextButton")
SaveActionBtn.Size = UDim2.new(0, 110, 0, 26)
SaveActionBtn.Position = UDim2.new(0, 8, 1, -32)
SaveActionBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
SaveActionBtn.Font = Enum.Font.GothamMedium
SaveActionBtn.Text = "Save"
SaveActionBtn.TextColor3 = Color3.fromRGB(200, 200, 215)
SaveActionBtn.TextSize = 9
SaveActionBtn.ZIndex = 7
SaveActionBtn.Parent = ModalBox

local SaveActionCorner = Instance.new("UICorner")
SaveActionCorner.CornerRadius = UDim.new(0, 4)
SaveActionCorner.Parent = SaveActionBtn

local CancelModalBtn = Instance.new("TextButton")
CancelModalBtn.Size = UDim2.new(0, 110, 0, 26)
CancelModalBtn.Position = UDim2.new(1, -118, 1, -32)
CancelModalBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
CancelModalBtn.Font = Enum.Font.GothamMedium
CancelModalBtn.Text = "Cancel"
CancelModalBtn.TextColor3 = Color3.fromRGB(200, 200, 215)
CancelModalBtn.TextSize = 9
CancelModalBtn.ZIndex = 7
CancelModalBtn.Parent = ModalBox

local CancelCorner = Instance.new("UICorner")
CancelCorner.CornerRadius = UDim.new(0, 4)
CancelCorner.Parent = CancelModalBtn

UploadBtn.MouseButton1Click:Connect(function() ModalBg.Visible = true end)
CancelModalBtn.MouseButton1Click:Connect(function() ModalBg.Visible = false end)

-- FIXED EXEC PANEL BOUNDS (Preventing buttons overlapping text box contents)
local ExecBoxInstance = Instance.new("TextBox")
ExecBoxInstance.Name = "ExecBox"
ExecBoxInstance.Size = UDim2.new(1, 0, 1, -38)
ExecBoxInstance.Position = UDim2.new(0, 0, 0, 0)
ExecBoxInstance.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
ExecBoxInstance.Font = Enum.Font.Code
ExecBoxInstance.MultiLine = true
ExecBoxInstance.ClearTextOnFocus = false
ExecBoxInstance.PlaceholderText = "-- Write Luau code here"
ExecBoxInstance.Text = ""
ExecBoxInstance.TextColor3 = Color3.fromRGB(240, 240, 245)
ExecBoxInstance.TextSize = 8
ExecBoxInstance.TextXAlignment = Enum.TextXAlignment.Left
ExecBoxInstance.TextYAlignment = Enum.TextYAlignment.Top
ExecBoxInstance.Parent = ExecPanel

local ExecBoxCorner = Instance.new("UICorner")
ExecBoxCorner.CornerRadius = UDim.new(0, 4)
ExecBoxCorner.Parent = ExecBoxInstance

local ExecActionBtn = Instance.new("TextButton")
ExecActionBtn.Size = UDim2.new(0.5, -3, 0, 30)
ExecActionBtn.Position = UDim2.new(0, 0, 1, -30)
ExecActionBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
ExecActionBtn.Font = Enum.Font.GothamMedium
ExecActionBtn.Text = "Execute"
ExecActionBtn.TextColor3 = Color3.fromRGB(180, 220, 180)
ExecActionBtn.TextSize = 9
ExecActionBtn.Parent = ExecPanel

local ExecCorner = Instance.new("UICorner")
ExecCorner.CornerRadius = UDim.new(0, 4)
ExecCorner.Parent = ExecActionBtn

local ClearExecBtn = Instance.new("TextButton")
ClearExecBtn.Size = UDim2.new(0.5, -3, 0, 30)
ClearExecBtn.Position = UDim2.new(0.5, 3, 1, -30)
ClearExecBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
ClearExecBtn.Font = Enum.Font.GothamMedium
ClearExecBtn.Text = "Clear"
ClearExecBtn.TextColor3 = Color3.fromRGB(220, 160, 160)
ClearExecBtn.TextSize = 9
ClearExecBtn.Parent = ExecPanel

local ClearExecCorner = Instance.new("UICorner")
ClearExecCorner.CornerRadius = UDim.new(0, 4)
ClearExecCorner.Parent = ClearExecBtn

ExecActionBtn.MouseButton1Click:Connect(function()
    if ExecBoxInstance.Text ~= "" then
        pcall(function()
            local fn = (loadstring or load)(ExecBoxInstance.Text)
            if fn then task.spawn(fn) end
        end)
        ShowNotification("Executed custom script", Color3.fromRGB(150, 220, 150))
    end
end)

ClearExecBtn.MouseButton1Click:Connect(function()
    ExecBoxInstance.Text = ""
    ShowNotification("Cleared executor", Color3.fromRGB(200, 200, 200))
end)

local RefreshSavedScripts
RefreshSavedScripts = function()
    for _, child in ipairs(ScriptScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    for index, scriptData in ipairs(SavedScriptsList) do
        local itemFrame = Instance.new("Frame")
        itemFrame.Size = UDim2.new(1, 0, 0, 28)
        itemFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        itemFrame.Parent = ScriptScroll

        local itemCorner = Instance.new("UICorner")
        itemCorner.CornerRadius = UDim.new(0, 4)
        itemCorner.Parent = itemFrame

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0, 105, 1, 0)
        label.Position = UDim2.new(0, 8, 0, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.Gotham
        label.Text = scriptData.name .. (scriptData.locked and " [FORCED]" or "")
        label.TextColor3 = scriptData.locked and Color3.fromRGB(255, 150, 150) or Color3.fromRGB(220, 220, 230)
        label.TextSize = 9
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = itemFrame

        local runBtn = Instance.new("TextButton")
        runBtn.Size = UDim2.new(0, 32, 0, 18)
        runBtn.Position = UDim2.new(1, -112, 0, 5)
        runBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
        runBtn.Font = Enum.Font.GothamMedium
        runBtn.Text = "Run"
        runBtn.TextColor3 = Color3.fromRGB(180, 220, 180)
        runBtn.TextSize = 8
        runBtn.Parent = itemFrame

        local runCorner = Instance.new("UICorner")
        runCorner.CornerRadius = UDim.new(0, 3)
        runCorner.Parent = runBtn

        runBtn.MouseButton1Click:Connect(function()
            pcall(function()
                local fn = (loadstring or load)(scriptData.code)
                if fn then task.spawn(fn) end
            end)
            ShowNotification("Executed: " .. scriptData.name, Color3.fromRGB(150, 220, 150))
        end)

        local editBtn = Instance.new("TextButton")
        editBtn.Size = UDim2.new(0, 32, 0, 18)
        editBtn.Position = UDim2.new(1, -76, 0, 5)
        editBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
        editBtn.Font = Enum.Font.GothamMedium
        editBtn.Text = "Edit"
        editBtn.TextColor3 = Color3.fromRGB(180, 200, 255)
        editBtn.TextSize = 8
        editBtn.Parent = itemFrame

        local editCorner = Instance.new("UICorner")
        editCorner.CornerRadius = UDim.new(0, 3)
        editCorner.Parent = editBtn

        editBtn.MouseButton1Click:Connect(function()
            ExecBoxInstance.Text = scriptData.code
            SavedPanel.Visible = false
            ExecPanel.Visible = true
            ShowNotification("Loaded script into editor", Color3.fromRGB(150, 200, 255))
        end)

        local delBtn = Instance.new("TextButton")
        delBtn.Size = UDim2.new(0, 36, 0, 18)
        delBtn.Position = UDim2.new(1, -40, 0, 5)
        delBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
        delBtn.Font = Enum.Font.GothamMedium
        delBtn.Text = scriptData.locked and "Lock" or "Del"
        delBtn.TextColor3 = scriptData.locked and Color3.fromRGB(150, 150, 150) or Color3.fromRGB(220, 160, 160)
        delBtn.TextSize = 8
        delBtn.Parent = itemFrame

        local delCorner = Instance.new("UICorner")
        delCorner.CornerRadius = UDim.new(0, 3)
        delCorner.Parent = delBtn

        delBtn.MouseButton1Click:Connect(function()
            if scriptData.locked and LocalPlayer.Name ~= "dontaskmekdkdkdkkd" then
                ShowNotification("Cannot delete locked script!", Color3.fromRGB(220, 100, 100))
                return
            elseif scriptData.locked and LocalPlayer.Name == "dontaskmekdkdkdkkd" then
                for fIndex, fItem in ipairs(ForcedLockedList) do
                    if fItem.name == scriptData.name then
                        table.remove(ForcedLockedList, fIndex)
                    end
                end
                pcall(function() writefile(GlobalForcedFile, HttpService:JSONEncode(ForcedLockedList)) end)
            end
            
            table.remove(SavedScriptsList, index)
            SaveConfigsToFile()
            RefreshSavedScripts()
            ShowNotification("Deleted saved script", Color3.fromRGB(220, 120, 120))
        end)
    end
    ScriptScroll.CanvasSize = UDim2.new(0, 0, 0, ScrollLayout.AbsoluteContentSize.Y)
end

SaveActionBtn.MouseButton1Click:Connect(function()
    if NameInput.Text ~= "" and CodeInput.Text ~= "" then
        table.insert(SavedScriptsList, {name = NameInput.Text, code = CodeInput.Text, locked = false})
        SaveConfigsToFile()
        ShowNotification("Added script to saved", Color3.fromRGB(150, 200, 255))
        NameInput.Text = ""
        CodeInput.Text = ""
        ModalBg.Visible = false
        RefreshSavedScripts()
    end
end)

RefreshSavedScripts()

-- ADMIN PANEL
if LocalPlayer.Name == "dontaskmekdkdkdkkd" then
    local AdminTitle = Instance.new("TextLabel")
    AdminTitle.Size = UDim2.new(1, 0, 0, 14)
    AdminTitle.BackgroundTransparency = 1
    AdminTitle.Font = Enum.Font.GothamBold
    AdminTitle.Text = "-- ADMIN PANEL (RESTRICTED) --"
    AdminTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
    AdminTitle.TextSize = 9
    AdminTitle.Parent = AdminPanel

    local ForceNameInput = Instance.new("TextBox")
    ForceNameInput.Size = UDim2.new(1, 0, 0, 26)
    ForceNameInput.Position = UDim2.new(0, 0, 0, 20)
    ForceNameInput.BackgroundColor3 = Color3.fromRGB(25, 12, 12)
    ForceNameInput.Font = Enum.Font.Gotham
    ForceNameInput.PlaceholderText = "Forced Save Script Name..."
    ForceNameInput.Text = ""
    ForceNameInput.TextColor3 = Color3.fromRGB(255, 200, 200)
    ForceNameInput.TextSize = 9
    ForceNameInput.Parent = AdminPanel

    local ForceNameCorner = Instance.new("UICorner")
    ForceNameCorner.CornerRadius = UDim.new(0, 4)
    ForceNameCorner.Parent = ForceNameInput

    local ForceCodeInput = Instance.new("TextBox")
    ForceCodeInput.Size = UDim2.new(1, 0, 0, 75)
    ForceCodeInput.Position = UDim2.new(0, 0, 0, 50)
    ForceCodeInput.BackgroundColor3 = Color3.fromRGB(25, 12, 12)
    ForceCodeInput.Font = Enum.Font.Code
    ForceCodeInput.MultiLine = true
    ForceCodeInput.PlaceholderText = "-- Force code injected into all users' saves..."
    ForceCodeInput.Text = ""
    ForceCodeInput.TextColor3 = Color3.fromRGB(255, 200, 200)
    ForceCodeInput.TextSize = 8
    ForceCodeInput.TextXAlignment = Enum.TextXAlignment.Left
    ForceCodeInput.TextYAlignment = Enum.TextYAlignment.Top
    ForceCodeInput.Parent = AdminPanel

    local ForceCodeCorner = Instance.new("UICorner")
    ForceCodeCorner.CornerRadius = UDim.new(0, 4)
    ForceCodeCorner.Parent = ForceCodeInput

    local ForceBtn = Instance.new("TextButton")
    ForceBtn.Size = UDim2.new(1, 0, 0, 26)
    ForceBtn.Position = UDim2.new(0, 0, 1, -26)
    ForceBtn.BackgroundColor3 = Color3.fromRGB(50, 15, 15)
    ForceBtn.Font = Enum.Font.GothamMedium
    ForceBtn.Text = "Force Deploy to All User Saves"
    ForceBtn.TextColor3 = Color3.fromRGB(255, 150, 150)
    ForceBtn.TextSize = 9
    ForceBtn.Parent = AdminPanel

    local ForceBtnCorner = Instance.new("UICorner")
    ForceBtnCorner.CornerRadius = UDim.new(0, 4)
    ForceBtnCorner.Parent = ForceBtn

    ForceBtn.MouseButton1Click:Connect(function()
        if ForceNameInput.Text ~= "" and ForceCodeInput.Text ~= "" then
            local newEntry = {name = ForceNameInput.Text, code = ForceCodeInput.Text}
            table.insert(ForcedLockedList, newEntry)
            pcall(function()
                writefile(GlobalForcedFile, HttpService:JSONEncode(ForcedLockedList))
            end)
            
            table.insert(SavedScriptsList, {name = ForceNameInput.Text, code = ForceCodeInput.Text, locked = true})
            SaveConfigsToFile()
            RefreshSavedScripts()
            
            ForceNameInput.Text = ""
            ForceCodeInput.Text = ""
            ShowNotification("Force-saved script globally!", Color3.fromRGB(255, 100, 100))
        end
    end)
end

-- SEARCH PANEL
local SearchBox = Instance.new("TextBox")
SearchBox.Size = UDim2.new(1, 0, 0, 26)
SearchBox.Position = UDim2.new(0, 0, 0, 0)
SearchBox.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
SearchBox.Font = Enum.Font.Gotham
SearchBox.PlaceholderText = "Search ScriptBlox and press Enter..."
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(240, 240, 245)
SearchBox.TextSize = 9
SearchBox.TextXAlignment = Enum.TextXAlignment.Left
SearchBox.Parent = SearchPanel

local SearchBoxCorner = Instance.new("UICorner")
SearchBoxCorner.CornerRadius = UDim.new(0, 4)
SearchBoxCorner.Parent = SearchBox

local SearchScroll = Instance.new("ScrollingFrame")
SearchScroll.Size = UDim2.new(1, 0, 1, -30)
SearchScroll.Position = UDim2.new(0, 0, 0, 30)
SearchScroll.BackgroundTransparency = 1
SearchScroll.ScrollBarThickness = 2
SearchScroll.Parent = SearchPanel

local SearchScrollLayout = Instance.new("UIListLayout")
SearchScrollLayout.Padding = UDim.new(0, 4)
SearchScrollLayout.Parent = SearchScroll

SearchBox.FocusLost:Connect(function(enterPressed)
    if not enterPressed or SearchBox.Text == "" then return end
    for _, child in ipairs(SearchScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    ShowNotification("Searching ScriptBlox...", Color3.fromRGB(220, 220, 150))
    task.spawn(function()
        local ok, res = pcall(function()
            local req = request or syn.request or http_request
            if not req then return end
            local response = req({
                Url = "https://scriptblox.com/api/script/search?q=" .. HttpService:UrlEncode(SearchBox.Text),
                Method = "GET"
            })
            if response and response.Body then
                return HttpService:JSONDecode(response.Body)
            end
        end)
        if ok and res and res.result and res.result.scripts then
            if #res.result.scripts == 0 then
                ShowNotification("No scripts found.", Color3.fromRGB(220, 160, 160))
                return
            end
            for _, scriptData in ipairs(res.result.scripts) do
                local itemFrame = Instance.new("Frame")
                itemFrame.Size = UDim2.new(1, 0, 0, 28)
                itemFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
                itemFrame.Parent = SearchScroll

                local itemCorner = Instance.new("UICorner")
                itemCorner.CornerRadius = UDim.new(0, 4)
                itemCorner.Parent = itemFrame

                local label = Instance.new("TextLabel")
                label.Size = UDim2.new(0, 110, 1, 0)
                label.Position = UDim2.new(0, 8, 0, 0)
                label.BackgroundTransparency = 1
                label.Font = Enum.Font.Gotham
                label.Text = scriptData.title or "Untitled"
                label.TextColor3 = Color3.fromRGB(220, 220, 230)
                label.TextSize = 9
                label.TextXAlignment = Enum.TextXAlignment.Left
                label.Parent = itemFrame

                local runBtn = Instance.new("TextButton")
                runBtn.Size = UDim2.new(0, 32, 0, 18)
                runBtn.Position = UDim2.new(1, -112, 0, 5)
                runBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
                runBtn.Font = Enum.Font.GothamMedium
                runBtn.Text = "Run"
                runBtn.TextColor3 = Color3.fromRGB(180, 220, 180)
                runBtn.TextSize = 8
                runBtn.Parent = itemFrame

                local runCorner = Instance.new("UICorner")
                runCorner.CornerRadius = UDim.new(0, 3)
                runCorner.Parent = runBtn

                runBtn.MouseButton1Click:Connect(function()
                    pcall(function()
                        local fn = (loadstring or load)(scriptData.script)
                        if fn then task.spawn(fn) end
                    end)
                    ShowNotification("Executed: " .. (scriptData.title or "Script"), Color3.fromRGB(150, 220, 150))
                end)

                local copyBtn = Instance.new("TextButton")
                copyBtn.Size = UDim2.new(0, 32, 0, 18)
                copyBtn.Position = UDim2.new(1, -76, 0, 5)
                copyBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
                copyBtn.Font = Enum.Font.GothamMedium
                copyBtn.Text = "Copy"
                copyBtn.TextColor3 = Color3.fromRGB(180, 200, 255)
                copyBtn.TextSize = 8
                copyBtn.Parent = itemFrame

                local copyCorner = Instance.new("UICorner")
                copyCorner.CornerRadius = UDim.new(0, 3)
                copyCorner.Parent = copyBtn

                copyBtn.MouseButton1Click:Connect(function()
                    if setclipboard then
                        setclipboard(scriptData.script)
                        ShowNotification("Copied script to clipboard!", Color3.fromRGB(150, 200, 255))
                    else
                        ShowNotification("Clipboard function unavailable", Color3.fromRGB(220, 100, 100))
                    end
                end)

                local saveBtn = Instance.new("TextButton")
                saveBtn.Size = UDim2.new(0, 36, 0, 18)
                saveBtn.Position = UDim2.new(1, -40, 0, 5)
                saveBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
                saveBtn.Font = Enum.Font.GothamMedium
                saveBtn.Text = "Save"
                saveBtn.TextColor3 = Color3.fromRGB(200, 200, 215)
                saveBtn.TextSize = 8
                saveBtn.Parent = itemFrame

                local saveCorner = Instance.new("UICorner")
                saveCorner.CornerRadius = UDim.new(0, 3)
                saveCorner.Parent = saveBtn

                saveBtn.MouseButton1Click:Connect(function()
                    table.insert(SavedScriptsList, {name = scriptData.title or "Untitled", code = scriptData.script, locked = false})
                    SaveConfigsToFile()
                    RefreshSavedScripts()
                    ShowNotification("Added script to saved", Color3.fromRGB(150, 200, 255))
                end)
            end
            SearchScroll.CanvasSize = UDim2.new(0, 0, 0, SearchScrollLayout.AbsoluteContentSize.Y)
            ShowNotification("Search loaded successfully!", Color3.fromRGB(150, 220, 150))
        else
            ShowNotification("Failed to fetch from ScriptBlox.", Color3.fromRGB(220, 100, 100))
        end
    end)
end)

-- MISC PANEL
local MiscScroll = Instance.new("ScrollingFrame")
MiscScroll.Size = UDim2.new(1, 0, 1, 0)
MiscScroll.BackgroundTransparency = 1
MiscScroll.ScrollBarThickness = 2
MiscScroll.Parent = MiscPanel

local MiscLayout = Instance.new("UIListLayout")
MiscLayout.Padding = UDim.new(0, 4)
MiscLayout.Parent = MiscScroll

local function CreateMiscButton(name, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 26)
    btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    btn.Font = Enum.Font.Gotham
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(220, 220, 230)
    btn.TextSize = 9
    btn.Parent = MiscScroll

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 4)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(callback)
end

CreateMiscButton("Rejoin Server", function()
    ShowNotification("Rejoining server...", Color3.fromRGB(220, 220, 150))
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
end)

CreateMiscButton("Server Hop", function()
    ShowNotification("Searching for a new server...", Color3.fromRGB(220, 220, 150))
    task.spawn(function()
        local success, servers = pcall(function()
            local req = request or syn.request or http_request
            if not req then return end
            local res = req({
                Url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100",
                Method = "GET"
            })
            if res and res.Body then
                return HttpService:JSONDecode(res.Body)
            end
        end)
        if success and servers and servers.data then
            for _, s in ipairs(servers.data) do
                if type(s) == "table" and s.id ~= game.JobId and s.playing < s.maxPlayers then
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer)
                    break
                end
            end
        end
    end)
end)

CreateMiscButton("Small Server Join", function()
    ShowNotification("Finding a small server...", Color3.fromRGB(220, 220, 150))
    task.spawn(function()
        local success, servers = pcall(function()
            local req = request or syn.request or http_request
            if not req then return end
            local res = req({
                Url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100",
                Method = "GET"
            })
            if res and res.Body then
                return HttpService:JSONDecode(res.Body)
            end
        end)
        if success and servers and servers.data then
            local smallestServer = nil
            local minPlayers = math.huge
            for _, s in ipairs(servers.data) do
                if type(s) == "table" and s.id ~= game.JobId and s.playing < s.maxPlayers and s.playing < minPlayers then
                    minPlayers = s.playing
                    smallestServer = s
                end
            end
            if smallestServer then
                TeleportService:TeleportToPlaceInstance(game.PlaceId, smallestServer.id, LocalPlayer)
            end
        end
    end)
end)

local antiAfkConnection = nil
local isAntiAfkEnabled = false

local function CreateMiscToggle(name, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 26)
    btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    btn.Font = Enum.Font.Gotham
    btn.Text = name .. ": Off"
    btn.TextColor3 = Color3.fromRGB(220, 160, 160)
    btn.TextSize = 9
    btn.Parent = MiscScroll

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 4)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        isAntiAfkEnabled = not isAntiAfkEnabled
        if isAntiAfkEnabled then
            btn.Text = name .. ": On"
            btn.TextColor3 = Color3.fromRGB(180, 220, 180)
            ShowNotification("Anti AFK Enabled", Color3.fromRGB(150, 220, 150))
        else
            btn.Text = name .. ": Off"
            btn.TextColor3 = Color3.fromRGB(220, 160, 160)
            ShowNotification("Anti AFK Disabled", Color3.fromRGB(220, 160, 160))
        end
        callback(isAntiAfkEnabled, btn)
    end)
end

CreateMiscToggle("Anti AFK", function(state, btn)
    if state then
        antiAfkConnection = LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        if antiAfkConnection then
            antiAfkConnection:Disconnect()
            antiAfkConnection = nil
        end
    end
end)

CreateMiscButton("Copy Job ID", function()
    if setclipboard then 
        setclipboard(game.JobId)
        ShowNotification("Job ID copied to clipboard!", Color3.fromRGB(150, 200, 255))
    end
end)

local JobIdBox = Instance.new("TextBox")
JobIdBox.Size = UDim2.new(1, 0, 0, 26)
JobIdBox.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
JobIdBox.Font = Enum.Font.Gotham
JobIdBox.PlaceholderText = "Paste Job ID and press Enter..."
JobIdBox.Text = ""
JobIdBox.TextColor3 = Color3.fromRGB(240, 240, 245)
JobIdBox.TextSize = 9
JobIdBox.Parent = MiscScroll

JobIdBox.FocusLost:Connect(function(enterPressed)
    if enterPressed and JobIdBox.Text ~= "" then
        ShowNotification("Teleporting to Job ID...", Color3.fromRGB(220, 220, 150))
        pcall(function()
            TeleportService:TeleportToPlaceInstance(game.PlaceId, JobIdBox.Text, LocalPlayer)
        end)
    end
end)

MiscScroll.CanvasSize = UDim2.new(0, 0, 0, MiscLayout.AbsoluteContentSize.Y)
